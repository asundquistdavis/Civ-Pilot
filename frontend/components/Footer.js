import React from "react";

const Footer = (props) => {

    const { } = props;

    return (
        <div style={{fontSize: '.7rem'}}> version: 2.0.0 - asundquistdavs@gmail.com </div>
    )
};

export default Footer;
