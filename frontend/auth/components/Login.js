import React from "react";
import Header from "../../components/Header";
import NewUserButton from "./NewUserButton";

function Login() {

    // const buttonL = NewUserButton.bind(this);

    return(
        <div>
            <Header buttonL={this.NewUserButton()} text="Mega Civilization"></Header>
        </div>
    )
};

export default Login;
